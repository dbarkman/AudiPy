# =============================================================================
# AudiPy Backend Configuration
# =============================================================================
# Copy this file to .env and fill in your actual values
# Usage: cp .env.example .env

# -----------------------------------------------------------------------------
# Database Configuration (Required for all CLI scripts)
# -----------------------------------------------------------------------------
DB_HOST=localhost
DB_PORT=3306
DB_NAME=audipy
DB_USER=audipy
DB_PASSWORD=your_database_password_here

# -----------------------------------------------------------------------------
# Encryption Configuration (Required for all CLI scripts)
# -----------------------------------------------------------------------------
# This key is used to encrypt Audible tokens in the database
# IMPORTANT: Generate a new key using the command below, then paste it here
# To generate a new key, run this Python command:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# 
# If you already have data encrypted with an existing key, DO NOT change this!
AUDIPY_MASTER_KEY=your_generated_fernet_key_here

# -----------------------------------------------------------------------------
# Web API Configuration (Optional - only needed if running the web server)
# -----------------------------------------------------------------------------
# Secret key for JWT tokens (used by web interface)
JWT_SECRET=your_jwt_secret_key_change_in_production

# Allowed CORS origins for React frontend
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# -----------------------------------------------------------------------------
# Connection Pool Settings (Optional - defaults are fine for CLI usage)
# -----------------------------------------------------------------------------
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30

# -----------------------------------------------------------------------------
# Notes:
# -----------------------------------------------------------------------------
# 1. CLI Scripts (phase1-4) only need: DB_* and AUDIPY_MASTER_KEY
# 2. Web API needs all of the above
# 3. Never commit the .env file to version control!
# 4. Keep your AUDIPY_MASTER_KEY safe - losing it means losing access to stored tokens
